# User context

Free-text context about the user, captured during the setup interview (Q6: "Anything else I should know about you or your audience?").

This gets loaded by `eval-relevance.md` as the primary signal for relevance scoring.

The user may edit this file at any time. The skill only reads it — never writes to it after initial setup, unless the user runs "change context" from the quick-confirm menu.

---

_(no context yet — will populate after first-run interview)_
